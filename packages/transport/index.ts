// Phase 2 (docs/build/PHASE-2.md) defines the Transport interface and its
// multipeer (iOS), nearby (Android) and mock (web, tests) implementations.
// Placeholder so the package exists, resolves and typechecks.

export {};
